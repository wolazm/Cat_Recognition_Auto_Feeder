# Core Libraries
tensorflow==2.12.0          # For machine learning model training and inference
opencv-python==4.8.0        # For real-time image processing and computer vision
numpy==1.24.3               # For numerical operations
pandas==2.0.3               # For data handling and logging
flask==2.3.2                # For the web dashboard
RPi.GPIO==0.7.1             # For controlling Raspberry Pi GPIO pins (motors and sensors)
scikit-learn==1.3.0         # For additional ML utilities (if needed)
pillow==10.0.0              # For image processing utilities

# Development Tools
matplotlib==3.7.2           # For visualizing training results (optional)
jupyter==1.0.0              # For interactive development (optional)
